# v0.8.5

- Added an editable Name field to the New Home flow.
- When you edit a custom home path before editing the name, Multihome now defaults the name to the path's final component, such as `marketfinch.com`.
- Matched the same path-based name fallback in `homeport clone --path` and `homeport create --path` when `--name` is omitted.
