# v0.13.1

- Fixed `homeport update` hanging while validating an existing bundled
  mitmproxy runtime. The installer now checks the terminating `mitmdump`
  version command instead of starting the long-running `mitmweb` server.
- Added an installer regression test that verifies the version probe never
  launches `mitmweb`, and runs in both CI and release validation.
