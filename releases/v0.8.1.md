# v0.8.1

- Prefilled the New Home path field with the normal managed destination under `~/.codex-homes/<slug>` so custom paths can be edited from a visible default.
- Kept the suggested path synced with the selected new-home mode until the user edits it manually.
