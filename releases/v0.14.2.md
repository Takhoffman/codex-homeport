# v0.14.2

- Fixed Repair Shim App from Finder-launched Multihome by exposing Homebrew's
  `node` alongside the resolved `npx` executable when extracting and repacking
  the Codex ASAR bundle.
- Added regression coverage for the minimal `PATH` inherited by macOS apps.
