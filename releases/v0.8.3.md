# v0.8.3

- Merged missing homes and launch instances from legacy `CodexHomeport` state even when current `CodexMultihome` state already exists.
- Restored visibility for older open homes, including active sessions launched by the previous Homeport app/profile namespace.
