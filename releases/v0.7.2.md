# Codex Multihome v0.7.2

- Replaced the managed-home delete system confirmation with an in-app confirmation sheet so the menu bar window stays open and the destructive action completes visibly.
- Added the optional `--move-folders` rename path for managed homes, including matching app profile folder moves and recent/source metadata updates.
- Added tests for rename folder moves, folder conflicts, and recent item name updates.
