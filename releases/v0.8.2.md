# v0.8.2

- Added an Open Now section in the menu bar and console so running Codex instances stay visible even when older than the most recent closed launches.
- Kept Recent Opens focused on closed/non-running launches so active sessions no longer get pushed out of the visible list.
