# v0.13.2

- Replaced executable runtime version probes with pinned SHA-256 validation of
  the official mitmproxy binaries for both Apple Silicon and Intel. This keeps
  `homeport update` from launching any bundled server executable while it
  validates an existing npm-installed runtime.
- Extended the installer regression fixture to verify checksum validation runs
  without ever executing `mitmweb`.
