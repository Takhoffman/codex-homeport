# v0.9.0

- Added per-home model routing controls for Codex Shim launches, with provider toggles for ChatGPT/Codex, Ollama, and Cursor.
- Added Browser local testing launch mode and propagated that mode into the Node runtime.
- Added Computer Use target defaults and setup support so desktop automation can reach apps macOS otherwise marks as forbidden.
- Added a launch profile control in the menu app and fixed CLI channel selection for dev/live installs.
- Improved Codex desktop bundle handling, including renamed/copied shim app bundles.
- Removed dead Grok and Claude CLI/subscription provider routes from the shim UI and generated catalog.
