# v0.8.4

- Launched Codex desktop homes through the `/Applications/Codex.app` bundle instead of executing the inner binary directly, so macOS permission prompts attach to Codex rather than a Multihome-launched helper identity.
- Kept per-home `CODEX_HOME`, isolated browser profile arguments, new-instance behavior, and active instance PID tracking for the Open Now view.
