# Codex Multihome v0.7.0

- Renamed the project, app, repository metadata, and npm package from Codex Homeport / `codex-homeport` to Codex Multihome / `codex-multihome`.
- Updated app bundle identity, support paths, install documentation, update checks, and npm install instructions for the new package name.
- Kept the `homeport` CLI command as a compatibility alias while the product branding moves to Multihome.
