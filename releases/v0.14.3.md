# v0.14.3

- Fixed Codex Shim homes missing Browser, Chrome, and Computer Use by hydrating
  bundled plugin caches and marketplace metadata from the main Codex home at
  launch.
- Enabled the bundled browser and computer plugins in shim configuration while
  preserving unrelated TOML tables from older managed blocks.
- Reused an already-running Codex Shim process instead of restarting it, which
  prevents live permission requests from becoming orphaned “Awaiting approval”
  tasks.
- Correctly detected renamed Codex Shim bundles whose executable is still named
  `ChatGPT`.
- Added regression coverage for plugin hydration, marketplace configuration,
  legacy configuration preservation, and shim process detection.
